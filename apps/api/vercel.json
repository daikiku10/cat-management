{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @repo/db build && pnpm --filter api build",
  "rewrites": [{ "source": "/api/:path*", "destination": "/api" }]
}
